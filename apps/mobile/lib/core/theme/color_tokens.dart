import 'package:flutter/widgets.dart';

/// Brand seed colors, unchanged from the Phase 1.1 placeholder shell.
abstract final class NebulaColorTokens {
  static const Color lightSeed = Color(0xFF315EFB);
  static const Color darkSeed = Color(0xFF89A7FF);
}
